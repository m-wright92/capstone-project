<% if @users %>
  <% @users.each do |user| %>
    <% if user != current_user %>
      <p>
        <%= link_to user.full_name, user_path(user) %>
      </p>
    <% end %>
  <% end %>
<% else %>
  <p>No users found</p>
<% end %>

